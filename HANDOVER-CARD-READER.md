# ดัชนีการค้นหาตลาดแรงงานอีสาน: การ์ดสำหรับคนใช้ข้อมูล

ใบนี้สำหรับคนที่เอาข้อมูลไปทำกราฟและเขียนรายงาน ไม่ต้องรู้วิธีเก็บข้อมูล
(คนที่รันรอบเก็บข้อมูลใช้อีกใบหนึ่ง)

---

## ข้อมูลนี้คืออะไร

คำค้นหาที่เกี่ยวกับการหางานและการออกจากงาน **50 คำ** เก็บจาก Google Trends เป็นรายเดือน
ระดับประเทศย้อนถึงปี **2004** ระดับภาคอีสานย้อนถึงปี **2014**

แต่ละคำถูกจัดกลุ่มด้วยสองแกน
- **Formal / Informal** งานในระบบ หรือ นอกระบบ
- **Pull / Push** แรงดึงเข้าตลาดแรงงาน (เช่น สมัครงาน หางาน) หรือ แรงผลักออก (เช่น ลาออกจากงาน ลงทะเบียนว่างงาน)

---

## เปิดใน Tableau

ไฟล์เดียวที่ต้องต่อคือ

```
derived\sa_pipeline_v3\series.csv
```

เป็นตารางยาว หนึ่งแถวคือหนึ่งเดือนของหนึ่งคำในหนึ่งพื้นที่ ต่อตรงได้เลยไม่ต้องแปลงอะไร
พอมีการเก็บข้อมูลรอบใหม่ ไฟล์นี้จะถูกสร้างใหม่ทั้งไฟล์ กด refresh ใน Tableau ก็เห็นเดือนล่าสุด

| คอลัมน์ | ใช้ตอนไหน |
|---|---|
| **`SA_Rebased`** | **คอลัมน์หลักที่ควรใช้** ปรับฤดูกาลแล้ว เทียบข้ามช่วงเวลาได้ |
| `MA3_Centered` | ดูทิศทางแนวโน้ม แต่ **จุดสุดท้ายยังไม่นิ่ง** ต้องรอข้อมูลเดือนถัดไปถึงจะสรุปได้ |
| `Input_Rebased` | ค่าก่อนปรับฤดูกาล ใช้ตอนอยากเทียบว่าการปรับฤดูกาลเปลี่ยนภาพไปแค่ไหน |
| `Case_Name_TH` | ชื่อคำค้นภาษาไทย ใช้เป็น label |
| `Segment` / `Factor` | Formal-Informal และ Pull-Push ใช้แบ่งกลุ่มในกราฟ |
| **`Quality_Status`** | **ลากไปที่ Filter ทุกครั้ง** ดูหัวข้อถัดไป |

## `Scope` มี 7 ค่า และไม่ได้เชื่อถือได้เท่ากัน

| Scope | คืออะไร | ผ่านเกณฑ์คุณภาพกี่ % |
|---|---|---|
| `TH` | ทั้งประเทศ | **32%** |
| `REG_ISAN5` | อีสานรวม 5 จังหวัด | 10% |
| `TH-30` | นครราชสีมา | **6%** |
| `TH-31` | บุรีรัมย์ | **6%** |
| `TH-34` | อุบลราชธานี | **6%** |
| `TH-40` | ขอนแก่น | **6%** |
| `TH-41` | อุดรธานี | **6%** |

**เส้นรายจังหวัดในไฟล์นี้หน้าตาเหมือนเส้นระดับประเทศทุกอย่าง แต่ 94% ไม่ผ่านเกณฑ์คุณภาพ**
เพราะจังหวัดเดียวมีคนค้นน้อยกว่ามาก เดือนที่ค่าเป็นศูนย์เลยเยอะจนโปรแกรมปรับฤดูกาลทำงานได้ไม่ดี

**เวลาทำกราฟรายจังหวัด ให้ลาก `Quality_Status` ไปที่ Filter เสมอ**

- `PASS` ใช้ได้ตามปกติ
- `REVIEW` ใช้ได้แต่ต้องเขียนคำกำกับ (ดูหัวข้อการปรับฤดูกาล)
- `NO_SIGNAL` **อย่าใช้** ไม่มีสัญญาณให้ปรับ

ถ้าอยากเทียบจังหวัดกันเอง ให้ดู `Input_Rebased` ซึ่งเป็นค่าก่อนปรับฤดูกาลแทน จะตรงไปตรงมากว่า

---

## 4 กติกาที่ห้ามพลาดตอนเขียนรายงาน

1. **ค่าเป็นดัชนี 0 ถึง 100 ไม่ใช่จำนวนคน**
   ห้ามเขียนว่ามีคนค้นกี่คนหรือกี่ครั้ง และในไฟล์นี้แต่ละคำถูกดึงแยกกัน จึงถูกปรับฐานคนละฐาน **เทียบขนาดข้ามคำไม่ได้** พูดได้แค่ว่าคำนี้เดือนนี้สูงหรือต่ำกว่าตัวมันเองในอดีต
   ถ้าอยากเทียบข้ามคำจริงๆ ต้องให้คนดูแลดึงใหม่โดยใส่คำพร้อมกันในกราฟเดียว

2. **เส้นอีสานเป็นค่าที่คำนวณขึ้น ไม่ได้เก็บจาก Google โดยตรง**
   มันคือค่าเฉลี่ยจากจังหวัดที่มีสัญญาณ ซึ่งบางคำมีไม่ครบ 5 จังหวัด ต้องดูเลข `N/5` ก่อนเสมอ **ถ้าไม่ครบ 5 ห้ามเขียนว่ารวม 5 จังหวัด**

3. **ไม่มีสัญญาณ ไม่ได้แปลว่าไม่มีคนหางาน**
   คำที่ขึ้นว่า no-data แปลว่าปริมาณค้นหาต่ำกว่าเกณฑ์ที่ Google ยอมรายงาน ส่วนคำที่ขึ้นว่า WEAK แปลว่ามีเดือนที่ค่าเป็นศูนย์เยอะ ทั้งสองอย่างเป็นข้อจำกัดของการวัด **ไม่ใช่หลักฐานว่าไม่มีอุปสงค์แรงงานในพื้นที่นั้น**

4. **ข้อมูลระดับจังหวัดเริ่มปี 2014 เท่านั้น**
   ระดับประเทศเริ่ม 2004 ได้ ห้ามลากเส้นจังหวัดย้อนไปก่อน 2014 เพราะ Google เปลี่ยนวิธีระบุตำแหน่งช่วงปี 2011 ถึง 2013 ข้อมูลก่อนหน้านั้นใช้ไม่ได้

---

## เรื่องการปรับฤดูกาล ต้องเขียนกำกับ

ในโฟลเดอร์เดียวกันมีไฟล์ `quality_flags.csv` เปิดควบกับ `series.csv` ได้

เคสที่ `Quality_Status` เป็น `REVIEW` **ยังใช้ได้** แต่แปลว่าค่าสถิติคุณภาพของโปรแกรมปรับฤดูกาล (X-13) ไม่ผ่านเกณฑ์มาตรฐาน ซึ่งเป็นเรื่องปกติของข้อมูลค้นหา เพราะเกณฑ์นั้นถูกออกแบบมาสำหรับข้อมูลผลผลิตและการจ้างงาน ไม่ใช่ดัชนี 0 ถึง 100 ที่ปัดเป็นจำนวนเต็มและมีเดือนที่เป็นศูนย์อยู่มาก

**เวลาอ้างตัวเลขให้เขียนกำกับว่า** "ปรับฤดูกาลด้วย X-13 โดยค่าสถิติคุณภาพยังไม่ผ่านเกณฑ์มาตรฐาน"
**อย่าเขียนลอยๆ ว่าปรับฤดูกาลแล้ว**

---

## ข้อควรรู้อีกสองข้อ

- **อย่าเปิดโฟลเดอร์ `data\` แล้วดูดไฟล์ในนั้นเข้า Tableau เอง** มันเป็นข้อมูลดิบที่ยังไม่ผ่านการประมวลผล ใช้ `series.csv` เท่านั้น
- **อย่าแก้ไฟล์ใดๆ ในโฟลเดอร์นี้** ถ้าอยากได้คอลัมน์เพิ่มหรือพื้นที่เพิ่ม ให้บอกคนดูแล

---

คนดูแลเครื่องมือนี้: ......................................... ติดต่อ: .........................................

เครื่องมือนี้จัดทำโดย Nitisart Srijunpho งานสหกิจศึกษา 2026
