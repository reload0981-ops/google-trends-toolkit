# Claude instructions

คู่มือปฏิบัติงานฉบับเต็มของ repo นี้อยู่ที่ **`SKILL.md`** อ่านไฟล์นั้นก่อนทำงานทุกครั้ง
กติกาเหล็กที่ย้ำซ้ำตรงนี้: ห้ามแก้ `data/` และ `data.js` ด้วยมือ (ใช้ `collector/ingest.py` หรือ `collector/collect.py` เท่านั้น) และห้ามต่อท่อนข้อมูล Google Trends คนละช่วงเวลาเข้าซีรีส์เดียว
