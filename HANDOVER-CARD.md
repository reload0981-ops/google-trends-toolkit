# Google Trends Toolkit ระบบแรงงานอีสาน

การ์ดสำหรับคนรันรอบเดือน ปริ้นแล้ววางข้างจอ รายละเอียดเต็มอยู่ใน `README.md`

---

## รอบเดือน 5 ขั้น

รันหลังวันที่ 1 ถึง 3 ของเดือน เพราะเดือนก่อนหน้าต้องจบสมบูรณ์ก่อน เผื่อเวลาไว้ราว 2 ชั่วโมง

1. ดับเบิลคลิก **`run-monthly-loop.cmd`** ที่โฟลเดอร์ repo รอจนขึ้น `QUEUE READY`
2. ใน Chrome คลิกไอคอน extension > **Open Controller** > **Import jobs.json** (เลือก `extension\data\jobs.json`) > **Start**
3. ปล่อยหน้าต่าง Chrome ไว้หน้าสุด **ห้าม minimize** เจอ CAPTCHA ให้แก้ในแท็บที่เด้ง แล้วกด **Resume**
4. เมื่อ Controller เหลือ **0 FAILED** กลับไปที่หน้าต่างสีดำ พิมพ์ **`FINISH`** แล้ว Enter
5. รอจนขึ้น **`MONTHLY CHECKS PASSED`** สีเขียว จบรอบ

> คำสั่งนี้ **ไม่ส่งอะไรขึ้นเว็บหรือขึ้น Git เลย** ผลอยู่บนเครื่องเรา กดผิดไม่ทำให้ของส่วนกลางพัง ลองได้

---

## หลังจบรอบ ต่อ Tableau

ไฟล์เดียวที่ต้องรู้จักคือ **`derived\sa_pipeline_v3\series.csv`** เป็น long format ต่อตรงได้ พอรอบเดือนจบไฟล์นี้จะถูกสร้างใหม่ทั้งไฟล์ กด refresh ใน Tableau ก็เห็นเดือนล่าสุด

| ใช้คอลัมน์ไหน | ตอนไหน |
|---|---|
| `SA_Rebased` | เทียบระดับข้ามช่วงเวลา ปรับฤดูกาลแล้ว **คอลัมน์หลักที่ควรใช้** |
| `MA3_Centered` | ดูทิศทางแนวโน้ม แต่ **จุดสุดท้ายยังไม่นิ่ง** ต้องรอข้อมูลเดือนถัดไป |
| `Input_Rebased` | ค่าก่อนปรับฤดูกาล ใช้ตอนอยากเทียบว่าการปรับฤดูกาลเปลี่ยนภาพไปมากแค่ไหน |

`Scope` มีแค่ `TH` (ประเทศ) กับ `REG_ISAN5` (อีสาน) **ไม่มีรายจังหวัดในไฟล์นี้** ถ้าอยากได้รายจังหวัดต้องเปิดหน้าเว็บ

**เวลาเขียนรายงาน** ให้เปิด `quality_flags.csv` ควบ เคสที่ `Quality_Status = REVIEW` ยังใช้ได้ แต่แปลว่าค่า Q ของ X-13 ไม่ผ่านเกณฑ์มาตรฐาน (ปกติของข้อมูลค้นหา เพราะเกณฑ์นั้นออกแบบมาสำหรับข้อมูลผลผลิตและการจ้างงาน) ถ้าจะอ้างตัวเลขให้เขียนกำกับว่า **"ปรับฤดูกาลด้วย X-13 โดยค่าสถิติคุณภาพยังไม่ผ่านเกณฑ์มาตรฐาน"** อย่าเขียนลอยๆ ว่าปรับฤดูกาลแล้วเฉยๆ

---

## อาการที่จะเจอ กับวิธีแก้

| อาการ | ทำยังไง |
|---|---|
| คิวจบแล้วแต่โฟลเดอร์ `incoming\` ว่าง | ที่เก็บดาวน์โหลดของ Chrome ไม่ได้ชี้มาที่ `incoming\` เช็ค `chrome://settings/downloads` แล้วกด **Reconcile Downloads** ใน Controller **ห้ามกด Import ซ้ำก่อนเด็ดขาด** เพราะ Import จะล้างสถานะรอบปัจจุบัน แล้ว Reconcile จะใช้ไม่ได้อีกเลย |
| CAPTCHA เด้ง | ปกติของงานชุดใหญ่ แก้ในแท็บที่เด้ง แล้วกด **Resume** ห้ามปิดหน้าต่าง |
| job FAIL หลายตัว | กด **Retry Failed/No Data** ก่อน ถ้าคำนั้นเงียบจริง ปล่อยให้ Controller สังเกตซ้ำจนขึ้น NO_DATA ครบ 2 ครั้ง ระบบจะออกไฟล์หลักฐานให้เอง |
| Retry แล้วยัง FAIL ซ้ำ ทั้งที่เปิดหน้า Google Trends แล้วมีข้อมูล | **หยุด แล้วติดต่อคนดูแล** อย่าลบคำออกจาก `keywords.csv` เอง เพราะจะทำให้ซีรีส์ขาดตอนโดยไม่มีร่องรอย |
| พิมพ์ FINISH แล้วขึ้นข้อความว่ามีไฟล์ต้อง review | มีไฟล์แปลกปลอมอยู่ใน `incoming\` **ไฟล์เดียวบล็อกทั้งรอบ** ต้องย้ายมันออกจาก `incoming\` เองก่อน แล้วพิมพ์ FINISH ใหม่ |
| พิมพ์ FINISH แล้ว freshness gate ไม่ผ่าน | ยังเก็บไม่ครบทุกคู่ กลับไปที่ Controller ให้เหลือ 0 FAILED ก่อน **ห้ามเผยแพร่ครึ่งๆ** |

ติดอะไรที่ไม่มีในตารางนี้ กด **Copy Debug** ใน Controller แล้วส่งข้อความนั้นมาก่อน อย่าเพิ่งพิมพ์บรรยายอาการ

---

## 4 กติกาการอ่านตัวเลข

1. ค่าเป็น **index 0 ถึง 100 ไม่ใช่จำนวนคน** ห้ามเขียนว่ามีคนค้นกี่คน และในไฟล์นี้แต่ละคำถูกดึงแยกกันจึง rebase คนละฐาน **เทียบขนาดข้ามคำไม่ได้** ถ้าอยากเทียบข้ามคำจริงต้องดึงใหม่โดยใส่คำพร้อมกันในกราฟเดียว
2. เส้น **อีสานเป็นค่าที่คำนวณขึ้น** ไม่ได้เก็บจาก Google โดยตรง ต้องดู `N/5` ทุกครั้ง ถ้าไม่ครบ 5 จังหวัด **ห้ามเรียกว่ารวม 5 จังหวัด**
3. **no-data** แปลว่าปริมาณค้นหาต่ำกว่าเกณฑ์ที่ Google ยอมรายงาน ส่วน **WEAK** เป็นเกณฑ์ของเครื่องมือนี้เอง นับจากจำนวนเดือนที่ค่าเป็นศูนย์ในรอบ 64 เดือน ทั้งสองอย่างคือข้อจำกัดของสัญญาณค้นหา **ไม่ใช่หลักฐานว่าไม่มีอุปสงค์แรงงาน**
4. **ข้อมูลระดับจังหวัดเริ่มปี 2014 เท่านั้น** (ระดับประเทศเริ่ม 2004) ห้ามลากเส้นจังหวัดย้อนไปก่อนหน้านั้น

---

## ข้อห้าม

1. **ห้ามแก้ไฟล์ใน `data\` และ `data.js` ด้วยมือ** ทุกอย่างต้องผ่าน extension แล้ว ingest เท่านั้น
2. **ห้ามอัพเดทด้วยช่วงเวลาสั้น** ต้องดึงใหม่ทั้งเส้น 2004 ถึงปัจจุบันเสมอ เพราะค่าเป็น index การต่อท่อนคนละช่วงทำให้สเกลเพี้ยนทั้งเส้น
3. **ห้าม `git pull`, `git push` หรือ `git add -A` เอง** คนดูแลเป็นคนเดียวที่อัพเดต Git ถ้าต้องการโค้ดเวอร์ชันใหม่ให้แจ้งคนดูแล เพราะ pull ทับข้อมูลที่เพิ่งเก็บจะทำให้ต้องเก็บใหม่ทั้งรอบ

---

ผู้ดูแล: Nitisart Srijunpho (ลีโอ) | repo: https://github.com/reload0981-ops/google-trends-toolkit
