# การ์ดรอบเดือน

ใบนี้คือสิ่งที่ทำเดือนละครั้ง ปริ้นแล้ววางข้างจอ
เรื่องอื่นทั้งหมด (ตั้งเครื่องใหม่ เพิ่มคำ เผยแพร่ ตอบคำถามเรื่องตัวเลข) อยู่ใน **`MAINTAINER-GUIDE.md`**
คนที่เอาข้อมูลไปทำกราฟอย่างเดียวใช้ **`HANDOVER-CARD-READER.md`**

---

## รอบเดือน 5 ขั้น

ทำหลังวันที่ 1 ถึง 3 ของเดือน เพราะต้องรอให้เดือนก่อนหน้าจบก่อน เผื่อเวลาไว้ราว 2 ชั่วโมง

**1.** เปิดโฟลเดอร์งาน แล้วดับเบิลคลิกไฟล์ชื่อ **`เริ่มเก็บข้อมูลเดือนนี้`**
หน้าต่างสีดำจะเด้งขึ้นมา รอจนขึ้นคำว่า **`QUEUE READY`** ตอนนั้นจะมีไฟล์ชื่อ **`queue-this-month.json`** โผล่บนหน้าจอ (Desktop)

**2.** ไปที่ Chrome กด**ไอคอนของเครื่องมือ**ที่อยู่ข้างช่องที่อยู่เว็บ แล้วกด **Open Controller**
ในหน้าต่างคุมงานที่เปิดขึ้นมา กด **Import jobs.json** แล้วเลือกไฟล์ **`queue-this-month.json`** ที่อยู่บนหน้าจอ จากนั้นกด **Start**

**3.** ปล่อยให้หน้าต่าง Chrome นั้นอยู่หน้าสุด **ห้ามย่อลงไป** ถ้ามีหน้าให้ยืนยันว่าไม่ใช่หุ่นยนต์ ให้ทำในแท็บที่เด้งขึ้นมา แล้วกด **Resume** ห้ามปิดหน้าต่าง

**4.** เมื่อในหน้าต่างคุมงานเหลือ **FAILED เป็น 0** กลับไปที่หน้าต่างสีดำ พิมพ์ **`FINISH`** แล้วกด Enter

**5.** รอจนขึ้นตัวหนังสือสีเขียวว่า **`MONTHLY CHECKS PASSED`** จบรอบ เปิด Tableau กด refresh ได้เลย

> 5 ขั้นนี้**ยังไม่ส่งอะไรออกไปให้ใครเห็น** ทุกอย่างอยู่ในเครื่องเรา
> การส่งขึ้นต้นฉบับส่วนกลางเป็นอีกขั้นหนึ่งที่ต้องตั้งใจทำ ดู `MAINTAINER-GUIDE.md` หัวข้อ "เผยแพร่"

---

## อาการที่จะเจอ กับวิธีแก้

| อาการ | ทำยังไง |
|---|---|
| งานวิ่งจบแล้ว แต่ไม่มีไฟล์เข้ามาเลย | กดปุ่ม **Reconcile Downloads** ในหน้าต่างคุมงาน **ห้ามกด Import ซ้ำก่อนเด็ดขาด** เพราะการ Import จะล้างสถานะรอบปัจจุบันทิ้ง แล้ว Reconcile จะใช้ไม่ได้อีกเลย ต้องเริ่มใหม่ทั้งรอบ |
| มีหน้าให้ยืนยันว่าไม่ใช่หุ่นยนต์ | เรื่องปกติของงานชุดใหญ่ ทำในแท็บที่เด้งขึ้น แล้วกด **Resume** ห้ามปิดหน้าต่าง |
| มีงานขึ้น FAILED หลายตัว | กด **Retry Failed/No Data** ก่อน ถ้าคำนั้นไม่มีคนค้นจริง ปล่อยให้ระบบลองซ้ำจนขึ้น NO_DATA ครบ 2 ครั้ง เดี๋ยวมันออกไฟล์หลักฐานให้เอง |
| กด Retry แล้วยัง FAILED ซ้ำ ทั้งที่เปิดหาคำนั้นใน Google Trends แล้วมีข้อมูล | เปิด `MAINTAINER-GUIDE.md` หัวข้อ **"เมื่อเจอสิ่งที่ไม่รู้จัก"** อย่าเพิ่งไปแก้รายการคำ |
| พิมพ์ FINISH แล้วขึ้นว่ามีไฟล์ต้อง review | มีไฟล์แปลกปลอมปนอยู่ **ไฟล์เดียวบล็อกทั้งรอบ** ต้องเอามันออกจากโฟลเดอร์ `incoming` ก่อน แล้วพิมพ์ FINISH ใหม่ |
| พิมพ์ FINISH แล้วขึ้นว่าข้อมูลยังไม่ถึงเดือนที่ต้องการ | ยังเก็บไม่ครบ กลับไปที่หน้าต่างคุมงานให้ FAILED เหลือ 0 ก่อน **ห้ามปล่อยครึ่งๆ** |

**ติดอะไรที่ไม่มีในตารางนี้** กด **Copy Debug** ในหน้าต่างคุมงานเก็บข้อความไว้ก่อน แล้วเปิด `MAINTAINER-GUIDE.md` หัวข้อ "เมื่อเจอสิ่งที่ไม่รู้จัก"

---

## ข้อห้ามระหว่างรอบเดือน

1. **ห้ามเปิดไฟล์ในโฟลเดอร์ `data` หรือไฟล์ `data.js` ขึ้นมาแก้เอง** ข้อมูลต้องเข้าผ่าน 5 ขั้นข้างบนเท่านั้น
2. **ห้ามไปโหลดข้อมูลช่วงสั้นๆ มาเติมเอง** ระบบต้องดึงใหม่ทั้งเส้นทุกครั้ง เพราะตัวเลขเป็นดัชนีที่เทียบกันภายในช่วงที่ดึง เอาคนละช่วงมาต่อกันแล้วเส้นจะเพี้ยนทั้งเส้น
3. **ห้ามแก้รายการคำระหว่างรอบเดือน** การเพิ่มหรือถอดคำทำให้ด่านตรวจแดงทันที ต้องทำเป็นงานแยกหลังรอบเดือนผ่านแล้ว ดู `MAINTAINER-GUIDE.md`
